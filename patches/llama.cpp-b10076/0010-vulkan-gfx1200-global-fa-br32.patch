diff --git a/ggml/src/ggml-vulkan/ggml-vulkan.cpp b/ggml/src/ggml-vulkan/ggml-vulkan.cpp
index c3fd436a..1f5a8679 100644
--- a/ggml/src/ggml-vulkan/ggml-vulkan.cpp
+++ b/ggml/src/ggml-vulkan/ggml-vulkan.cpp
@@ -3506,7 +3506,6 @@ static vk_fa_tuning_params get_fa_tuning_params_scalar(const vk_device& device,
 }

 static vk_fa_tuning_params get_fa_tuning_params_coopmat1(const vk_device& device, uint32_t hsk, uint32_t hsv, uint32_t n_rows, uint32_t n_kv, ggml_type k_type, ggml_type v_type, bool f32acc) {
-    GGML_UNUSED(n_rows);
     GGML_UNUSED(n_kv);
     GGML_UNUSED(k_type);
     GGML_UNUSED(v_type);
@@ -3517,7 +3516,15 @@ static vk_fa_tuning_params get_fa_tuning_params_coopmat1(const vk_device& device

     const uint32_t D = hsk | hsv;

-    const uint32_t coopmat_block_rows = 16;
+    // Gemma 4 global-attention layers use 512-wide heads. On GFX1200,
+    // process two 16-row cooperative-matrix tiles per workgroup when the
+    // prefill batch is large enough. This doubles Q reuse while preserving the
+    // upstream Bc=64 online-softmax and direct K/V load path.
+    const bool gfx1200_global_prefill =
+        device->vendor_id == VK_VENDOR_ID_AMD &&
+        device->properties.deviceID == 0x7590 &&
+        hsk == 512 && hsv == 512 && n_rows >= 32;
+    const uint32_t coopmat_block_rows = gfx1200_global_prefill ? 32 : 16;
     const uint32_t coopmat_block_cols = 16;

     const uint32_t num_subgroups = 4;
@@ -10378,7 +10385,7 @@ static bool ggml_vk_flash_attn_coopmat_shmem_support(const vk_device& device, co
     // BF16 PVMat accumulator is f32 (no bf16 accumulator support), so pvsh is vec4 (16 bytes)
     const uint32_t pvsh_elem_size = (k_type == GGML_TYPE_BF16) ? 16u : f16vec4;
     const uint32_t osh_stride = params.row_split * MatBr / 4;
-    const uint32_t pvsh = MatBc * osh_stride * pvsh_elem_size;
+    const uint32_t pvsh = Br * osh_stride * pvsh_elem_size;

     const uint32_t slope = Br * acctype;

diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/flash_attn_cm1.comp b/ggml/src/ggml-vulkan/vulkan-shaders/flash_attn_cm1.comp
index 16178e57..149704c4 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/flash_attn_cm1.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/flash_attn_cm1.comp
@@ -58,7 +58,7 @@ const uint vsh_stride = v_cols;
 shared FLOAT_TYPEV4 kvsh[(kvsh_stride >= vsh_stride) ? (Bc * kvsh_stride) : (Bc * vsh_stride)];

 const uint32_t osh_stride = row_split * MatBr / 4;
-shared O_TYPEV4 pvsh[MatBc * osh_stride];
+shared O_TYPEV4 pvsh[Br * osh_stride];

 shared ACC_TYPE slope[Br];

@@ -252,7 +252,8 @@ void main() {
         // K * Q^T -> S^T: Bc x HSK_pad * HSK_pad x Br -> Bc x Br
         // Bc split across workgroup (four subgroups), loop over HSK in chunks of 16: 16 x 16 * 16 x 16 -> 16 x 16
         // This is written transposed in order to allow for N being 8 if implementations need it
-        coopmat<ACC_TYPE, gl_ScopeSubgroup, MatBc, MatBr, gl_MatrixUseAccumulator> SfMat = coopmat<ACC_TYPE, gl_ScopeSubgroup, MatBc, MatBr, gl_MatrixUseAccumulator>(0);
+        coopmat<ACC_TYPE, gl_ScopeSubgroup, MatBc, MatBr, gl_MatrixUseAccumulator> SfMat0 = coopmat<ACC_TYPE, gl_ScopeSubgroup, MatBc, MatBr, gl_MatrixUseAccumulator>(0);
+        coopmat<ACC_TYPE, gl_ScopeSubgroup, MatBc, MatBr, gl_MatrixUseAccumulator> SfMat1 = coopmat<ACC_TYPE, gl_ScopeSubgroup, MatBc, MatBr, gl_MatrixUseAccumulator>(0);
         coopmat<FLOAT_TYPE, gl_ScopeSubgroup, MatBc, 16, gl_MatrixUseA> KMat;
         coopmat<FLOAT_TYPE, gl_ScopeSubgroup, 16, MatBr, gl_MatrixUseB> QMat;

@@ -305,12 +306,19 @@ void main() {
             }

             coopMatLoad(QMat, Qf, d * 16 / 4, qstride, gl_CooperativeMatrixLayoutColumnMajor);
+            SfMat0 = coopMatMulAdd(KMat, QMat, SfMat0);

-            SfMat = coopMatMulAdd(KMat, QMat, SfMat);
+            if (Br > MatBr) {
+                coopMatLoad(QMat, Qf, MatBr * qstride + d * 16 / 4, qstride, gl_CooperativeMatrixLayoutColumnMajor);
+                SfMat1 = coopMatMulAdd(KMat, QMat, SfMat1);
+            }
         }

         uint coord = gl_SubgroupID * MatBc * sfshstride;
-        coopMatStore(SfMat, sfsh, coord, sfshstride, gl_CooperativeMatrixLayoutRowMajor);
+        coopMatStore(SfMat0, sfsh, coord, sfshstride, gl_CooperativeMatrixLayoutRowMajor);
+        if (Br > MatBr) {
+            coopMatStore(SfMat1, sfsh, coord + MatBr / 4, sfshstride, gl_CooperativeMatrixLayoutRowMajor);
+        }
         barrier();

         if (LOGIT_SOFTCAP) {
@@ -425,7 +433,8 @@ void main() {
         [[unroll]] for (uint32_t hsv_tile = 0; hsv_tile < num_hsv_tiles; ++hsv_tile) {
             const uint hsv_offset = (hsv_tile * row_split + gl_SubgroupID) * 16;

-            coopmat<O_TYPE, gl_ScopeSubgroup, MatBc, MatBr, gl_MatrixUseAccumulator> PVMat = coopmat<O_TYPE, gl_ScopeSubgroup, MatBc, MatBr, gl_MatrixUseAccumulator>(0);
+            coopmat<O_TYPE, gl_ScopeSubgroup, MatBc, MatBr, gl_MatrixUseAccumulator> PVMat0 = coopmat<O_TYPE, gl_ScopeSubgroup, MatBc, MatBr, gl_MatrixUseAccumulator>(0);
+            coopmat<O_TYPE, gl_ScopeSubgroup, MatBc, MatBr, gl_MatrixUseAccumulator> PVMat1 = coopmat<O_TYPE, gl_ScopeSubgroup, MatBc, MatBr, gl_MatrixUseAccumulator>(0);

             // Preload V tiles for [Bc, 16 * num subgroups]
             const uint v_rows = Bc;
@@ -491,11 +500,19 @@ void main() {
                         coopMatLoad(QMat, kvsh, v_tile_offset, kvsh_stride, gl_CooperativeMatrixLayoutRowMajor);
                     }

-                    PVMat = coopMatMulAdd(KMat, QMat, PVMat);
+                    PVMat0 = coopMatMulAdd(KMat, QMat, PVMat0);
+                    if (Br > MatBr) {
+                        coopMatLoad(KMat, Psh, bc_chunk * MatBc * psh_stride + MatBr / 4, psh_stride, gl_CooperativeMatrixLayoutColumnMajor);
+                        PVMat1 = coopMatMulAdd(KMat, QMat, PVMat1);
+                    }
                 }

-                // Store PVMat to pvsh and load into Of
-                coopMatStore(PVMat, pvsh, o_offset, osh_stride, gl_CooperativeMatrixLayoutRowMajor);
+                // Store both 16-row query tiles. K/V cooperative loads above
+                // are shared by the two accumulators in the persistent Br=32 path.
+                coopMatStore(PVMat0, pvsh, o_offset, osh_stride, gl_CooperativeMatrixLayoutRowMajor);
+                if (Br > MatBr) {
+                    coopMatStore(PVMat1, pvsh, MatBr * osh_stride + o_offset, osh_stride, gl_CooperativeMatrixLayoutRowMajor);
+                }
             }

             barrier();
